# Welcome to your ShareLoop project

```
## Documentation
- Capstone / report notes may live under [`docs/`](docs/) (if present).
## License
Private / as specified by your course or organization.
Here’s what was added in README.md:

What ShareLoop is and main features (vendor / customer / Explore / RLS).
Tech stack table (React, Vite, Supabase, Tailwind, Leaflet, etc.).
Quick start: npm install, npm run dev on port 8080, plus build, preview, lint, test.
Supabase: where the client lives, security note about not committing keys and using env vars for forks.
Database: run supabase/migrations in order; note on pickup-hours add/drop; keep types.ts in sync.
Storage bucket product-images and pointers to the relevant migrations.
Geocoding pointer to src/lib/geocode.ts.
Routing table for /, /explore, dashboards.
Vercel deploy outline + SPA rewrites via vercel.json.
Folder layout and a short license line.
Replace git clone <your-repo-url> with your real GitHub URL when you publish. If you want .env.example with VITE_SUPABASE_* (no secrets), say so and we can add it in Agent mode.
